import getMuiTheme from 'material-ui/styles/getMuiTheme';

const defaultTheme = getMuiTheme({
  bottomNavigation: {
    selectedColor: '#00a3fe',
    unselectedColor: 'gray'
  },
  palette: {
    primary1Color: '#00A3FE',
  }
});

export default defaultTheme;